import { MathResultPipe } from './math-result.pipe';

describe('MathResultPipe', () => {
  it('create an instance', () => {
    const pipe = new MathResultPipe();
    expect(pipe).toBeTruthy();
  });
});
