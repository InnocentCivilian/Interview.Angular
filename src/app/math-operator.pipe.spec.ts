import { MathOperatorPipe } from './math-operator.pipe';

describe('MathOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new MathOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
